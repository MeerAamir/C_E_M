# Client Environment Variables (Example)
# Copy this file to .env.production and update with actual values for production deployment

# API Base URL
# For local development, use: http://localhost:5000
# For production (Netlify + Render), use your deployed backend URL
REACT_APP_API_URL=http://localhost:5000
